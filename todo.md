
- create a master list of items that will be usable in lists, recipes, and pantry. The idea is that for commonly used items (ie: eggs) we could create the item once and then reusable. It could be included in searches and filters, added to lists, etc.

- make a location attribute that will attach to each item: store, aisle, aisle_sequence. We can define that list in the Family tab and make it available in the grocery list.

- add the list management function to the family tab and only show the grocery list in the main part of the app. Add a "favorite" feature if the Family has more than one list to indicate which will show on the first tab.